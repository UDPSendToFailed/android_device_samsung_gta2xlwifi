# AOSP allows relabelto checkin_data_file but not relabelfrom.
# Needed when the dir already has checkin_data_file label and vold
# needs to update MLS categories during user unlock (prepareUserStorage).
allow vold_prepare_subdirs checkin_data_file:dir relabelfrom;
