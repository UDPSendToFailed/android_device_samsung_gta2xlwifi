allow kernel block_device:dir search;

allow kernel debug_block_device:blk_file rw_file_perms;

allow kernel sysfs:file { open read };

# Param block device access
allow kernel paramblk_device:blk_file { read write open getattr };

# kdevtmpfs device node creation
allow kernel device:dir { write add_name };
